<img width="1525" height="657" alt="데분4_1" src="https://github.com/user-attachments/assets/4981a913-d550-4d23-873d-01a459255f56" />
# 데이터분석 4주차 정규과제

📌데이터분석 정규과제는 매주 정해진 분량의 『*혼자 공부하는 데이터 분석 with 파이썬*』 을 읽고 학습하는 것입니다. 이번 주는 아래의 **DataAnalysis_4th_TIL**에 나열된 분량을 읽고 공부하시면 됩니다.

아래의 문제를 풀어보며 학습 내용을 점검하세요. 문제를 해결하는 과정에서 개념을 스스로 정리하고, 필요한 경우 제시된 강의를 참고하여 보완하는 것이 좋습니다.

<!-- 강의 링크는 아래와 같습니다.
https://www.youtube.com/watch?v=HNlRYQnLkek&list=PLVsNizTWUw7FGzSRCkQrPEEe-ljVXgS7k&index=8
https://www.youtube.com/watch?v=Cbk_tQtuhbM&list=PLVsNizTWUw7FGzSRCkQrPEEe-ljVXgS7k&index=9
-->


## DataAnalysis_4th_TIL

### 4장 데이터 요약하기
#### 01. 통계로 요약하기
#### 02. 분포 요약하기


## Study Schedule

| 주차  | 공부 범위     | 완료 여부 |
| ----- | ------------- | --------- |
| 1주차 | p.24~81    | ✅         |
| 2주차 | p.84~151   | ✅         |
| 3주차 | p.154~219  | ✅         |
| 4주차 | p.222~279 | ✅         |
| 5주차 | p.282~325 | 🍽️         |
| 6주차 | p.328~379 | 🍽️         |
| 7주차 | p.382~430 | 🍽️         |

<br>

<!-- 여기까진 그대로 둬 주세요-->


# 1️⃣ 개념 정리 

## 01. 통계로 요약하기

# 기술통계 개념 정리

## 1. 기술통계란?

**기술통계(Descriptive Statistics)**는 데이터의 특징을 숫자로 요약하여 설명하는 방법입니다. 데이터가 얼마나 있는지, 값이 주로 어디에 모여 있는지, 얼마나 퍼져 있는지 등을 파악할 때 사용합니다.

예를 들어 도서별 대출건수가 수십만 개 있다면, 모든 값을 하나씩 읽는 대신 평균·중앙값·표준편차 등을 확인하여 전체적인 특징을 이해할 수 있습니다.

## 2. 데이터의 중심을 나타내는 값

### 평균

모든 값을 더한 뒤 데이터의 개수로 나눈 값입니다.

```text
평균 = 모든 값의 합 ÷ 데이터 개수
```

전체적인 수준을 파악하기 좋지만, 유난히 크거나 작은 값의 영향을 받습니다. 대출건수가 대부분 5~10회여도 일부 도서가 수백 회 대출되었다면 평균이 높아질 수 있습니다.

* 판다스 코드: `ns_book7['대출건수'].mean()`

### 중앙값

데이터를 크기순으로 정렬했을 때 가운데에 위치하는 값입니다. 데이터 개수가 짝수라면 가운데 두 값의 평균을 사용합니다.

극단적으로 크거나 작은 값의 영향을 평균보다 적게 받습니다. 따라서 평균과 중앙값을 함께 보면 데이터가 한쪽으로 치우쳐 있는지 살펴보는 데 도움이 됩니다.

* 판다스 코드: `ns_book7['대출건수'].median()`

### 최빈값

데이터에서 가장 자주 등장하는 값입니다. 숫자뿐 아니라 도서명, 출판사 같은 문자 데이터에도 사용할 수 있습니다.

* 판다스 코드: `ns_book7['도서명'].mode()`
* 가장 많이 등장하는 값이 여러 개라면 여러 값이 반환될 수 있습니다.

## 3. 데이터의 범위와 위치를 나타내는 값

### 최솟값과 최댓값

각각 데이터에서 가장 작은 값과 가장 큰 값입니다. 값의 전체 범위를 확인하거나 예상과 다른 값이 있는지 살펴볼 때 유용합니다.

* 최솟값: `ns_book7['대출건수'].min()`
* 최댓값: `ns_book7['대출건수'].max()`

### 분위수

데이터를 크기순으로 정렬했을 때 특정 위치에 해당하는 값입니다.

| 분위수     | 의미                        |
| ------- | ------------------------- |
| 25% 분위수 | 전체 데이터의 약 25%가 이 값 이하에 위치 |
| 50% 분위수 | 중앙값                       |
| 75% 분위수 | 전체 데이터의 약 75%가 이 값 이하에 위치 |

25%, 50%, 75% 위치의 값을 **사분위수**라고 합니다. 분위수를 사용하면 값이 어떤 구간에 주로 분포하는지 확인할 수 있습니다.

* 판다스 코드: `ns_book7['대출건수'].quantile([0.25, 0.5, 0.75])`
* 데이터의 개수가 적거나 해당 위치에 값이 정확히 없으면, 두 값 사이를 계산한 결과가 나올 수 있습니다.

## 4. 데이터가 퍼진 정도를 나타내는 값

### 분산

각 데이터가 평균에서 얼마나 떨어져 있는지를 나타내는 값입니다. 값들이 평균 근처에 모여 있으면 분산이 작고, 넓게 퍼져 있으면 분산이 큽니다.

계산할 때 평균과 각 값의 차이를 제곱하므로, 결과의 단위도 원래 단위의 제곱이 됩니다.

* 판다스 코드: `ns_book7['대출건수'].var()`

### 표준편차

분산에 제곱근을 취한 값입니다. 분산과 마찬가지로 데이터의 퍼진 정도를 나타내지만, **원래 데이터와 단위가 같아** 해석하기가 더 쉽습니다.

예를 들어 대출건수의 표준편차는 ‘회’ 단위로 표시됩니다. 다만 표준편차가 19회라고 해서 모든 도서의 대출건수가 반드시 평균에서 19회 이내에 있다는 뜻은 아닙니다.

* 판다스 코드: `ns_book7['대출건수'].std()`

## 5. 통계량을 한꺼번에 확인하는 방법

`describe()`는 여러 기술통계량을 한 번에 보여주는 판다스 메서드입니다.

```python
ns_book7.describe()
```

기본적으로 숫자 열의 데이터 개수(`count`), 평균(`mean`), 표준편차(`std`), 최솟값(`min`), 사분위수, 최댓값(`max`)을 출력합니다. 여기서 **`count`는 결측치를 제외한 개수**입니다.

문자 열의 요약 정보는 다음과 같이 확인합니다.

```python
ns_book7.describe(include='object')
```

문자 열에서는 고유한 값의 개수(`unique`), 가장 자주 등장하는 값(`top`), 그 값의 등장 횟수(`freq`) 등을 확인할 수 있습니다.

## 6. 평균을 해석할 때 확인할 점

같은 데이터라도 **무엇을 한 단위로 볼 것인지**에 따라 평균의 의미가 달라집니다.

* 도서별 대출건수의 평균: 각 도서를 하나의 데이터로 보고 계산
* 도서별 권당 대출건수의 평균: 각 도서의 `대출건수 ÷ 도서권수`를 구한 뒤 평균
* 전체 도서 한 권당 대출건수: `전체 대출건수의 합 ÷ 전체 도서권수의 합`

세 값은 계산 방법과 의미가 서로 다릅니다. 분석 결과에 평균을 사용할 때는 숫자만 제시하지 말고 **어떤 기준으로 계산했는지** 함께 밝혀야 합니다.

## 7. 판다스와 NumPy의 분산 계산 차이

판다스의 `var()`와 NumPy의 `np.var()`는 기본 계산 방식이 다릅니다.

| 함수               |    기본 설정 | 계산할 때 나누는 수 |
| ---------------- | -------: | ----------: |
| 판다스 `var()`      | `ddof=1` |  데이터 개수 − 1 |
| NumPy `np.var()` | `ddof=0` |      데이터 개수 |

따라서 같은 데이터로 계산해도 결과가 조금 다를 수 있습니다. `std()` 역시 같은 차이가 있습니다. 두 라이브러리의 결과를 비교할 때는 `ddof` 설정을 확인해야 합니다.


## 02. 분포 요약하기

1. 산점도

산점도는 두 숫자형 변수의 관계를 점으로 나타낸 그래프입니다. 가로축(x)에 한 변수, 세로축(y)에 다른 변수를 놓고, 데이터의 각 행을 점 하나로 표시합니다.

예를 들어 가로축을 도서권수, 세로축을 대출건수로 지정하면 각 점은 도서 한 종류의 도서권수와 대출건수를 나타냅니다.

점들이 오른쪽 위로 향하면 양의 관계가 있을 가능성이 있습니다.
점들이 오른쪽 아래로 향하면 음의 관계가 있을 가능성이 있습니다.
점이 많이 겹치면 alpha=0.1처럼 투명도를 낮춰 밀집된 곳을 확인할 수 있습니다.

주의: 대출건수 ÷ 도서권수와 대출건수를 비교할 때 두 값에 모두 대출건수가 들어갑니다. 따라서 그래프에서 함께 증가하는 모습이 보이더라도, 이를 독립적인 두 요인 사이의 관계로 해석해서는 안 됩니다.

2. 히스토그램

히스토그램은 숫자형 데이터 하나를 여러 구간으로 나누고, 각 구간에 값이 몇 개 있는지 막대 높이로 나타낸 그래프입니다. 구간에 속한 데이터의 개수를 도수라고 합니다.

plt.hist(ns_book7['대출건수'], bins=100)

여기서 bins=100은 값의 범위를 100개 구간으로 나눈다는 뜻입니다. 구간 수가 달라지면 그래프 모양도 달라질 수 있습니다.

대출건수처럼 작은 값에 데이터가 몰리고 일부 값만 매우 클 때는 큰 막대 때문에 나머지가 잘 보이지 않을 수 있습니다. 이때 plt.yscale('log')를 사용하면 세로축의 눈금 간격을 로그 방식으로 바꿔 작은 막대도 살펴보기 쉬워집니다. 실제 데이터의 개수가 바뀌는 것은 아닙니다.

3. 상자 수염 그림

상자 수염 그림은 데이터의 중심, 퍼진 정도, 큰 값과 작은 값을 간결하게 보여주는 그래프입니다.

부분	의미
상자의 아래쪽	제1사분위수: 25% 위치
상자 안의 선	중앙값: 50% 위치
상자의 위쪽	제3사분위수: 75% 위치
상자의 높이	사분위범위(IQR) = 제3사분위수 − 제1사분위수
수염	설정된 범위 안에서 가장 멀리 있는 데이터까지 표시
수염 밖의 점	설정된 범위를 벗어난 데이터

기본 설정에서는 사분위범위의 1.5배를 기준으로 수염을 그립니다. 수염 밖의 점을 흔히 이상치라고 부르지만, 그 점이 오류라는 뜻은 아닙니다. 실제로 대출이 많이 된 도서일 수도 있으므로 데이터의 의미를 확인한 뒤 처리해야 합니다.

도서권수처럼 25%, 50%, 75% 위치의 값이 모두 같다면 상자의 높이가 0이어서 상자가 선처럼 보일 수 있습니다.

4. 그래프를 그릴 때 사용하는 주요 코드
코드	역할
plt.scatter(x, y)	산점도
plt.hist(값, bins=구간수)	히스토그램
plt.boxplot(값)	상자 수염 그림
alpha=0.1	겹친 점을 확인하기 위한 투명도
plt.xscale('log')	가로축을 로그 눈금으로 표시
plt.yscale('log')	세로축을 로그 눈금으로 표시
plt.show()	그래프를 화면에 출력


# 2️⃣ 수행 인증


<img width="996" height="592" alt="데분4_5" src="https://github.com/user-attachments/assets/cbb8cbc3-bf20-4e2f-b725-0f76b15ec265" />
<img width="1306" height="592" alt="데분4_4" src="https://github.com/user-attachments/assets/6e18fcdb-6c10-4f94-8ef7-b617f69d47a9" />
<img width="1470" height="536" alt="데분4_3" src="https://github.com/user-attachments/assets/1307db01-9e13-4a29-a3c9-aca3aad32c7e" />
<img width="1525" height="657" alt="데분4_1" src="https://github.com/user-attachments/assets/351519d0-8f12-42f4-960c-de1f060e1da1" />
<img width="1407" height="617" alt="데분4_2" src="https://github.com/user-attachments/assets/ea383b72-8a2c-4ec3-a853-219b14af3435" />


<br>
<br>

# 3️⃣ 확인 문제

## 문제 1.

> **🧚Q. 이번 주차에는 확인문제 대신 실습 과제를 진행합니다. 캐글에서 원하는 데이터셋을 선택하여 기술통계를 계산하고, 다양한 시각화를 수행해보세요.
작업은 코랩에서 진행한 뒤, 코랩 링크를 아래에 첨부해주세요.**

```
여기에 코랩 링크를 첨부해주세요!
(제출 전, 코랩의 공유 설정을 ‘링크가 있는 모든 사용자가 보기 가능’으로 변경했는지 반드시 확인해주세요.)
```



### 🎉 수고하셨습니다.
